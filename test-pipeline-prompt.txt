Test the blog content generation pipeline in my Django project at C:\Users\josep\Desktop\FullStackPortfolio

The pipeline has 3 steps that I need you to test individually:

1. Idea Generation - Fetches trending topics from Hacker News + RSS feeds, then uses GPT-5-nano to generate 3 blog topic ideas
2. Content Creation - Takes a pending idea and uses GPT-5.2 to write a full blog post (thought leadership, no code)
3. Proofreading - Uses GPT-5-mini to polish grammar and style

To test, run these commands in the terminal (make sure venv is activated first):

Step 1: Generate ideas
python manage.py shell -c "from apps.blog.application.content_generation.tasks import generate_daily_ideas; print(generate_daily_ideas())"

Step 2: Create a blog post from one idea  
python manage.py shell -c "from apps.blog.application.content_generation.tasks import process_pending_idea; print(process_pending_idea())"

Step 3: Verify the post was created
python manage.py shell -c "from apps.blog.infrastructure.models import BlogPostModel; post = BlogPostModel.objects.last(); print(f'Title: {post.title}') if post else print('No posts found')"

Run each step and show me the output. The full pipeline takes 1-2 minutes due to API calls.
