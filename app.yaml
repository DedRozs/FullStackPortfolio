runtime: python312  # Change to your Python version (e.g., python311)
entrypoint: gunicorn -b :$PORT portfolio.wsgi


instance_class: F2  # Choose an instance class based on your needs

service: default

env_variables:
  DJANGO_SETTINGS_MODULE: "portfolio.settings"
  SECRET_KEY: "django-insecure-&c-s+$47t3otj6!(h_d^qcjyn9ukqfjq+*u2a_ux)asa3(x_f2"
  DATABASE_URL: "mysql://portfolioApp:JuanPaco1993%23@217.196.48.82:3306/Portfolio"

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 5

handlers:
  - url: /static
    static_dir: static/

  - url: /.*
    script: auto
